# Guitar-Isolated-Splitter

Design for a guitar pedal that aims to split a single guitar signal, into two, electronicly isolated signals. It also contains a FET-based 'soft' mute switch for each channel.
